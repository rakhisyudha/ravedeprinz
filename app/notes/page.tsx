import { Page } from '../../components/Page'; import { notes } from '../../data/content';
export default function Notes(){return <Page eyebrow="TRANSMISSIONS / 005" title="NOTES" intro="Short transmissions from the workbench. Mostly unfinished thoughts, left legible on purpose."><div className="max-w-4xl">{notes.map((n,i)=><article className="group grid gap-5 border-t border-[#79D7FD]/20 py-8 md:grid-cols-[115px_1fr_100px]" key={n.title}><span className="mono text-xs text-[#79D7FD]">{n.date}</span><div><h2 className="display text-4xl font-bold transition group-hover:text-[#00BBFA]">{n.title}</h2><p className="mt-3 max-w-xl text-sm leading-7 text-[#9abaca]">{n.text}</p></div><span className="eyebrow text-right">{n.tag}</span></article>)}</div></Page>}
